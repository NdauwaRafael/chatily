import React, { Fragment, useEffect } from "react";

const Pages = () => {
    useEffect(() => {
        document.title = 'Welcome | Chat App';
    });
    return (
        <Fragment>

        </Fragment>    
    );
}

export default Pages;